#include <stdio.h>

/**
 * main - course an infinite loop
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	printf("infinite loop comming:(\n");

	i =0;
	/*
	 * while (i < 10)
	 * {
	 * putchar(i)
	 * }
	 */
	printf("infinite loop avoided! //o\n");

	return (0);
}
