#include "main.h"

/**
 * _strlen - return the length of the string
 *@str: string
 *
 * return : length
 */
size_t _strlen(const char *str)
{
	size_t_len = 0;

	while(*str**)
		len++;

	return (len);
}
